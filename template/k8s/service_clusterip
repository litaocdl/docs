apiVersion: v1
kind: Service
metadata:
  name: nginxsvc_clusterip
  labels:
    app: nginx
spec:
  type: ClusterIP
  ports:
  - port: 80
    protocol: TCP
    name: http
  - port: 443
    protocol: TCP
    name: https
  selector:
    name: nginx
